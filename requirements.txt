# ============================================
# Stock Database - Minimal Requirements
# Only packages actually used in the project
# ============================================

# Core data processing libraries
pandas>=1.5.0
numpy>=1.21.0

# Database
duckdb>=0.8.0

# Data source providers
jqdatasdk>=1.8.0      # JQData API for Chinese stocks
tushare>=1.2.89       # Tushare API for Beijing Stock Exchange

# Data models and validation
pydantic>=1.10.0

# Configuration management
pyyaml>=6.0

# API Server dependencies
flask>=2.0.0
flask-cors>=3.0.0

# ============================================
# Notes:
# - Removed unused packages: sklearn, matplotlib, seaborn, plotly,
#   statsmodels, scipy, click, tqdm, toml, jsonschema, cachetools, retrying
# - Removed concurrent-futures (built-in in Python 3.2+)
# - Removed typing-extensions (built-in in Python 3.8+)
# - Dependencies like python-dateutil will be auto-installed by pandas/jqdatasdk
# ============================================

# Development and testing tools (optional - uncomment if needed)
# pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=22.0.0
# flake8>=5.0.0
# mypy>=0.991
